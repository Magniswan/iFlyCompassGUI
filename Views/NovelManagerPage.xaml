<Page
    x:Class="iFlyCompassGUI.Views.NovelManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:iFlyCompassGUI.ViewModels">
    
    <Grid Background="Transparent" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="小说阅读器" FontSize="24" FontWeight="Bold" Margin="0,0,0,16"/>
        
        <CommandBar Grid.Row="1" DefaultLabelPosition="Right" Margin="0,0,0,8">
            <AppBarButton Label="导入小说" Command="{Binding ImportNovelCommand}">
                <AppBarButton.Icon>
                    <FontIcon Glyph="&#xE710;"/>
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
        
        <ListView Grid.Row="2" ItemsSource="{Binding Novels}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Grid Padding="4,8" ColumnDefinitions="Auto,*,Auto">
                        <FontIcon Grid.Column="0" Glyph="&#xE8F2;" FontSize="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding}" VerticalAlignment="Center" FontSize="14"/>
                        <Button Grid.Column="2" Content="删除" FontSize="12" Padding="12,4" Click="OnDeleteNovelClick" Tag="{Binding}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" 
                    Spacing="8" Visibility="{Binding Novels.Count, Converter={StaticResource IntToVisibilityConverter}, ConverterParameter=0}">
            <FontIcon Glyph="&#xE8F2;" FontSize="48" Opacity="0.3" HorizontalAlignment="Center"/>
            <TextBlock Text="暂无小说，点击上方导入按钮添加" Opacity="0.5" HorizontalAlignment="Center"/>
        </StackPanel>
        
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" VerticalAlignment="Bottom" Opacity="0.6"/>
    </Grid>
</Page>
