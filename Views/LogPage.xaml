<Page
    x:Class="iFlyCompassGUI.Views.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:iFlyCompassGUI.ViewModels"
    xmlns:models="using:iFlyCompassGUI.Models">
    
    <Grid Background="Transparent" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="日志" FontSize="24" FontWeight="Bold" Margin="0,0,0,8"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                <Button Command="{Binding ClearLogsCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE74C;" FontSize="14"/>
                        <TextBlock Text="清空"/>
                    </StackPanel>
                </Button>
                <ToggleSwitch Header="自动滚动" IsOn="{Binding AutoScroll, Mode=TwoWay}" 
                              OffContent="关" OnContent="开"/>
            </StackPanel>
        </Grid>
        
        <Border Grid.Row="2" Background="{StaticResource CardAcrylicBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="8" Padding="8">
            <ListView x:Name="LogListView" ItemsSource="{Binding LogEntries}" 
                      SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:LogEntry">
                        <Grid Padding="2,4" ColumnDefinitions="Auto,Auto,*">
                            <TextBlock Grid.Column="0" Text="{Binding Timestamp, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:HH:mm:ss}'}" 
                                       FontFamily="Consolas" Opacity="0.5" Width="60" VerticalAlignment="Center" FontSize="12"/>
                            <Border Grid.Column="1" Background="{Binding Level, Converter={StaticResource LevelToColorConverter}}" 
                                    CornerRadius="4" Padding="6,2" Margin="4,0">
                                <TextBlock Text="{Binding Level}" FontSize="10" FontWeight="Bold" 
                                           Foreground="White" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap" 
                                       FontFamily="Consolas" FontSize="12" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
