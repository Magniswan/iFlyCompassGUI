<Page
    x:Class="iFlyCompassGUI.Views.LogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:iFlyCompassGUI.ViewModels"
    xmlns:models="using:iFlyCompassGUI.Models">

    <Grid Background="Transparent" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="日志" FontSize="24" FontWeight="Bold" Margin="0,0,0,8"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                <Button Command="{Binding ClearLogsCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE74C;" FontSize="14"/>
                        <TextBlock Text="清空"/>
                    </StackPanel>
                </Button>
                <ToggleSwitch Header="自动滚动" IsOn="{Binding AutoScroll, Mode=TwoWay}"
                              OffContent="关" OnContent="开"/>
                <ComboBox Header="日志来源" ItemsSource="{Binding LogSources}"
                          SelectedItem="{Binding SelectedSource, Mode=TwoWay}"
                          MinWidth="100"/>
                <ComboBox Header="日志级别" ItemsSource="{Binding LogLevels}"
                          SelectedItem="{Binding SelectedLevel, Mode=TwoWay}"
                          MinWidth="100"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefault}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="8" Padding="0">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="12,8,12,8">
                <ItemsControl x:Name="LogItemsControl" ItemsSource="{Binding FilteredLogEntries}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="models:LogEntry">
                            <Grid Padding="0,1" ColumnDefinitions="Auto,Auto,*">
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Timestamp, Converter={StaticResource StringFormatConverter}, ConverterParameter='{}{0:HH:mm:ss} '}"
                                           FontFamily="Cascadia Code,Consolas" FontSize="12"
                                           Foreground="#FF666666" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding SourceTag}"
                                           FontFamily="Cascadia Code,Consolas" FontSize="12"
                                           Foreground="#FF999999" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding Message}"
                                           TextWrapping="Wrap" FontFamily="Cascadia Code,Consolas" FontSize="12"
                                           Foreground="{Binding Level, Converter={StaticResource LevelToTerminalColorConverter}}"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
